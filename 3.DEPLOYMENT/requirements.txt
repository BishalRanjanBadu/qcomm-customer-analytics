# PINNED TO THE PROMOTED ARTIFACT, NOT TO LATEST.
# Source: models/v_20260909T1228Z_nogit/manifest.json library_versions.
# These four produced the pickle. Serving on any other version risks a cross-version
# unpickle that appears to succeed and silently returns wrong segments.
# Verified 2026-09-09 against the PyPI JSON API: all exist with cp313 manylinux wheels
# for x86_64 and aarch64.
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.6.1
scipy==1.16.3
# scikit-learn's runtime dependencies. Left unpinned they resolve with >=, so two builds a
# week apart are not identical — which defeats the parity contract the four pins above
# exist to create. Verified 2026-09-09 on PyPI; both are pure-python wheels.
joblib==1.6.0
threadpoolctl==3.6.0

# Serving. Latest verified on PyPI 2026-09-09.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
boto3==1.43.90
